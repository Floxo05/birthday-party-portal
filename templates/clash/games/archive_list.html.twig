{% extends 'base.html.twig' %}

{% block title %}Clash – Spielearchiv{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-3">Spielearchiv</h1>
        <p class="text-muted">Hier kannst du alle Spiele üben. Deine Highscores zählen NICHT für den Clash.</p>

        {% if games is empty %}
            <div class="alert alert-secondary">Keine Spiele gefunden.</div>
        {% else %}
            <div class="row g-3">
                {% for g in games %}
                    <div class="col-12 col-md-6 col-lg-4">
                        <div class="card h-100">
                            <div class="card-body d-flex flex-column">
                                <div class="d-flex align-items-start justify-content-between mb-2">
                                    <div class="fs-3" aria-hidden="true">{{ g.icon ?? '🎮' }}</div>
                                    <span class="badge bg-secondary text-uppercase">Archiv</span>
                                </div>
                                <h5 class="card-title">{{ g.title }}</h5>
                                <p class="card-text text-muted flex-grow-1">{{ g.description }}</p>
                                <div class="d-grid gap-2 mt-2">
                                    <a href="{{ path('party_clash_game_detail_archive', { id: party.id, slug: g.slug }) }}"
                                       class="btn btn-outline-primary">Zur Archiv-Spielseite</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}

        <div class="mt-4 d-grid gap-2 d-md-block">
            <a href="{{ path('party_clash_games', { id: party.id }) }}" class="btn btn-outline-secondary">Zurück zu den aktiven Spielen</a>
            <a href="{{ path('party_clash_start', { id: party.id }) }}" class="btn btn-outline-secondary">Zurück zum Clash</a>
        </div>
    </div>
{% endblock %}
